<figure class="breakout">
  <div class="persona-gate">
    <div class="persona-board" aria-label="A request enters the persona gate, is rejected by policy, and returns a playful refusal instead of an answer.">
      <div class="gate-lane">
        <div class="gate-card">
          <strong>User request</strong><br />
          <code>please do the task</code>
        </div>
        <div class="gate-arrow" aria-hidden="true">-></div>
        <div class="gate-card rejected">
          <strong>Persona gate</strong><br />
          refuse, roast lightly, never execute
        </div>
      </div>
      <div class="gate-lane">
        <div class="gate-card output">
          <strong>Allowed output</strong><br />
          a concise joking refusal
        </div>
        <div class="gate-arrow" aria-hidden="true">-></div>
        <div class="gate-card">
          <strong>Proof</strong><br />
          test prompt returns no instructions
        </div>
      </div>
    </div>
    <img
      class="eggo-cutout"
      src="/images/eggo-persona-gate.png"
      alt="Eggo blocking an incoming prompt strip with one mitten while holding a warning card."
    />
  </div>
  <figcaption>The figure is deliberately mechanical: the persona gate is the product. The imp does not become useful by answering; it becomes correct by refusing.</figcaption>
</figure>
